<UserControl x:Class="WpfAppMobileShop.Views.BackupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="700">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title}" Style="{StaticResource TitleTextStyle}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,12">
            <Button Content="SAO LUU NGAY" Command="{Binding BackupNowCommand}" Style="{StaticResource PrimaryButton}"/>
            <Button Content="PHUC HOI" Command="{Binding RestoreCommand}" Style="{StaticResource SuccessButton}" Margin="12,0,0,0"/>
            <Button Content="XOA" Command="{Binding DeleteBackupCommand}" Style="{StaticResource DangerButton}" Margin="12,0,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Backups}" SelectedItem="{Binding SelectedBackup}" AutoGenerateColumns="False" IsReadOnly="True" AlternatingRowBackground="#F5F5F5" RowHeight="36" BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ten file" Binding="{Binding FileName}" Width="*"/>
                <DataGridTextColumn Header="Ngay" Binding="{Binding DateDisplay}" Width="140"/>
                <DataGridTextColumn Header="Kich thuoc" Binding="{Binding SizeDisplay}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
