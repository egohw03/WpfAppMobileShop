<UserControl x:Class="WpfAppMobileShop.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:helpers="clr-namespace:WpfAppMobileShop.Helpers"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <helpers:BoolToVisibilityConverter x:Key="InverseBoolToVis" Invert="True"/>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title}" Style="{StaticResource TitleTextStyle}"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,12">
            <Border Width="300" CornerRadius="4" BorderBrush="#DDD" BorderThickness="1" Padding="0">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="36"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <md:PackIcon Kind="Magnify" FontSize="16" Foreground="#999" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SearchBoxStyle}" BorderThickness="0"/>
                </Grid>
            </Border>
            <Button Content="THÊM" Command="{Binding AddCommand}" Style="{StaticResource SuccessButton}" Margin="12,0,0,0"/>
        </StackPanel>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Customers}" 
                  SelectedItem="{Binding SelectedCustomer}" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  AlternatingRowBackground="#F5F5F5" RowHeight="40" BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã" Binding="{Binding CustomerId}" Width="60"/>
                <DataGridTextColumn Header="Họ tên" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="SĐT" Binding="{Binding Phone}" Width="130"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Địa chỉ" Binding="{Binding Address}" Width="*"/>
                <DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedDate, StringFormat=dd/MM/yyyy}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
        <md:Card Grid.Row="3" Padding="16" Margin="0,12,0,0" UniformCornerRadius="8"
                 Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Họ tên:" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding EditingCustomer.FullName}" Style="{StaticResource SearchBoxStyle}"/>
                    <TextBlock Grid.Column="2"/>
                    <TextBlock Grid.Column="3" Text="SĐT:" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="4" Text="{Binding EditingCustomer.Phone}" Style="{StaticResource SearchBoxStyle}"/>
                </Grid>
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Email:" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding EditingCustomer.Email}" Style="{StaticResource SearchBoxStyle}"/>
                    <TextBlock Grid.Column="2"/>
                    <TextBlock Grid.Column="3" Text="Địa chỉ:" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="4" Text="{Binding EditingCustomer.Address}" Style="{StaticResource SearchBoxStyle}"/>
                </Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="LƯU" Command="{Binding SaveCommand}" 
                            Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>
                    <Button Content="HỦY" Command="{Binding CancelCommand}" 
                            Style="{StaticResource DangerButton}"/>
                </StackPanel>
            </StackPanel>
        </md:Card>
        <Border Grid.Row="4" Margin="0,12,0,0" Padding="12"
                Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVis}}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="XÓA" Command="{Binding DeleteCommand}" 
                        Style="{StaticResource DangerButton}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>