<UserControl x:Class="WpfAppMobileShop.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="900">
    <ScrollViewer>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding Title}" FontSize="28" FontWeight="Bold" Foreground="#1976D2"/>
            <Grid Grid.Row="1" Margin="0,20,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="White" CornerRadius="5" Padding="15" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Tổng đơn hàng" FontSize="14" Foreground="#666"/>
                        <TextBlock Text="{Binding TotalOrders}" FontSize="32" FontWeight="Bold" Foreground="#1976D2"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Background="White" CornerRadius="5" Padding="15" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Tồn kho" FontSize="14" Foreground="#666"/>
                        <TextBlock Text="{Binding TotalProducts}" FontSize="32" FontWeight="Bold" Foreground="#4CAF50"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Background="White" CornerRadius="5" Padding="15" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Khách hàng" FontSize="14" Foreground="#666"/>
                        <TextBlock Text="{Binding TotalCustomers}" FontSize="32" FontWeight="Bold" Foreground="#FF9800"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="3" Background="White" CornerRadius="5" Padding="15" Margin="5">
                    <StackPanel>
                        <TextBlock Text="Doanh thu hôm nay" FontSize="14" Foreground="#666"/>
                        <TextBlock Text="{Binding TodayRevenue, StringFormat=N0}" FontSize="32" FontWeight="Bold" Foreground="#F44336"/>
                    </StackPanel>
                </Border>
            </Grid>
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="White" CornerRadius="5" Padding="15" Margin="5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Doanh thu 7 ngày" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        <lvc:CartesianChart Grid.Row="1" Series="{Binding RevenueSeries}" 
                                            HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                            Hoverable="True">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Labels="{Binding RevenueLabels}" FontSize="10"/>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Title="Doanh thu" FontSize="10"/>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Grid>
                </Border>
                <Border Grid.Column="1" Background="White" CornerRadius="5" Padding="15" Margin="5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Top 5 sản phẩm bán chạy" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        <lvc:PieChart Grid.Row="1" Series="{Binding TopProductsSeries}" 
                                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                      Hoverable="True"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
