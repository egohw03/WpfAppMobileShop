<UserControl x:Class="WpfAppMobileShop.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title}" Style="{StaticResource TitleTextStyle}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,12">
            <DatePicker SelectedDate="{Binding FromDate}" Width="140" Style="{StaticResource DatePickerFilledStyle}"/>
            <TextBlock Text="đến" VerticalAlignment="Center" Margin="8,0"/>
            <DatePicker SelectedDate="{Binding ToDate}" Width="140" Style="{StaticResource DatePickerFilledStyle}"/>
        </StackPanel>

        <md:Card Grid.Row="2" Padding="16" UniformCornerRadius="8" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Tong don" FontSize="13" Foreground="{StaticResource SidebarTextBrush}"/>
                    <TextBlock Text="{Binding OrderCount}" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Doanh thu" FontSize="13" Foreground="{StaticResource SidebarTextBrush}"/>
                    <TextBlock Text="{Binding TotalRevenue, StringFormat=N0}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Giam gia" FontSize="13" Foreground="{StaticResource SidebarTextBrush}"/>
                    <TextBlock Text="{Binding TotalDiscount, StringFormat=N0}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource WarningBrush}"/>
                </StackPanel>
                <StackPanel Grid.Column="3">
                    <TextBlock Text="Thanh tien" FontSize="13" Foreground="{StaticResource SidebarTextBrush}"/>
                    <TextBlock Text="{Binding TotalFinal, StringFormat=N0}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>
                </StackPanel>
            </Grid>
        </md:Card>

        <TabControl Grid.Row="3">
            <TabItem Header="Chi tiet don hang">
                <DataGrid ItemsSource="{Binding Orders}" AutoGenerateColumns="False" IsReadOnly="True" AlternatingRowBackground="#F5F5F5" RowHeight="32" BorderThickness="0" Margin="4">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ma" Binding="{Binding OrderId}" Width="60"/>
                        <DataGridTextColumn Header="Ngay" Binding="{Binding OrderDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                        <DataGridTextColumn Header="KH" Binding="{Binding Customer.FullName}" Width="150"/>
                        <DataGridTextColumn Header="Tong" Binding="{Binding TotalAmount, StringFormat=N0}" Width="100"/>
                        <DataGridTextColumn Header="Giam" Binding="{Binding DiscountAmount, StringFormat=N0}" Width="80"/>
                        <DataGridTextColumn Header="Thanh toan" Binding="{Binding FinalAmount, StringFormat=N0}" Width="100"/>
                        <DataGridTextColumn Header="TT" Binding="{Binding Status}" Width="80"/>
                        <DataGridTextColumn Header="NV" Binding="{Binding User.FullName}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Top san pham">
                <DataGrid ItemsSource="{Binding TopProducts}" AutoGenerateColumns="True" IsReadOnly="True" AlternatingRowBackground="#F5F5F5" RowHeight="32" BorderThickness="0" Margin="4"/>
            </TabItem>
        </TabControl>

        <Button Grid.Row="4" Content="XUAT CSV" Command="{Binding ExportCsvCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" Margin="0,12,0,0"/>
    </Grid>
</UserControl>
