<UserControl x:Class="WpfAppMobileShop.Views.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:helpers="clr-namespace:WpfAppMobileShop.Helpers"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <helpers:BoolToVisibilityConverter x:Key="InverseBoolToVis" Invert="True"/>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title}" Style="{StaticResource TitleTextStyle}"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,12">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     Style="{StaticResource SearchBoxStyle}" Width="300"/>
            <Button Content="THÊM" Command="{Binding AddCommand}" 
                    Style="{StaticResource SuccessButton}" Margin="12,0,0,0"/>
        </StackPanel>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Categories}" 
                  SelectedItem="{Binding SelectedCategory}" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  AlternatingRowBackground="#F5F5F5" RowHeight="40" BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã" Binding="{Binding CategoryId}" Width="60"/>
                <DataGridTextColumn Header="Tên danh mục" Binding="{Binding CategoryName}" Width="*"/>
                <DataGridTextColumn Header="Mô tả" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <md:Card Grid.Row="3" Padding="16" Margin="0,12,0,0" UniformCornerRadius="8"
                 Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Tên danh mục:" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding EditingCategory.CategoryName}" 
                             Style="{StaticResource SearchBoxStyle}"/>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Mô tả:" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding EditingCategory.Description}" 
                             Style="{StaticResource SearchBoxStyle}"/>
                </Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                    <Button Content="LƯU" Command="{Binding SaveCommand}" 
                            Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>
                    <Button Content="HỦY" Command="{Binding CancelCommand}" 
                            Style="{StaticResource DangerButton}"/>
                </StackPanel>
            </StackPanel>
        </md:Card>
        <Border Grid.Row="4" Margin="0,12,0,0" Padding="12"
                Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVis}}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="XÓA" Command="{Binding DeleteCommand}" 
                        Style="{StaticResource DangerButton}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>